 echo bonjour |;

echo "\s" & echo "\s"

cat Makefile | grep pr | head -n 5 | cd test (mybin) // mauvaise valeur de retour exp2 -> act0

echo bonjour >>> test // manque message d'erreur et mauvaise valeur de retour

echo bonjour > > out // mauvaise valeur de retour

export var; export var=test // mauvaise valeur de retour

echo bonjour > $test // check valeur de retour

export "test=ici"=coucou // manque 1 char a la fin 
				--> bash = test=ici=coucou
				--> nous = test=ici"=couco

exit -10 -->bash sort 246, nous 255

check valeur de retour :  echo hudifg d | | hugdfihd
					--> attendu (2)

echo hi";" hihi, pas bon print

export "HI= hi"

export "HI =hi" // pas bonne valeur de retour

export U="s -a" 

https://github.com/ybelatar/minishell_destruction/blob/main/destruction.txt